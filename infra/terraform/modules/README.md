# Terraform Modules

Local modules belong here when infrastructure blocks become shared or meaningfully reusable. The current root module keeps resources together because the GECX app, Cloud Functions, BigQuery seed script, and toolset wiring are tightly coupled for the prototype.
